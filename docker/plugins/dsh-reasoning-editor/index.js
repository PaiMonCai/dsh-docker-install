/**
 * Host half intentionally has no runtime behavior.
 *
 * The feature is a Web client adapter registered through DSH's official
 * settings.models.provider-card slot. Settings remain owned by llm-pi-ai.
 */
export const inject = []

export function apply() {}
